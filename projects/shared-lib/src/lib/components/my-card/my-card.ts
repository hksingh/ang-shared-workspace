import { Component, Directive, ViewContainerRef, TemplateRef, Input } from '@angular/core';

@Component({
  selector: 'lib-my-card',
  imports: [],
  templateUrl: './my-card.html',
  styleUrl: './my-card.scss',
})
export class MyCard {}
